# Design

<!-- impeccable:design-schema 1 -->

빌드된 결과물에서 기록했다. 의도가 아니라 코드에 실제로 들어간 값이다.
토큰의 단일 출처는 `app/globals.css`이고, Tailwind는 그 CSS 변수를 읽기만 한다
(`tailwind.config.ts`의 `token()` 헬퍼).

## 방향

**제품 릴리스 피드.** 프로젝트마다 실제 화면이 먼저 오고 설명이 뒤따른다.
가운데 정렬 히어로와 3열 카드 그리드라는 카테고리 기본형을 거부한다.

전체 계약은 아래에 옮겨 적었다. 원래는 `app/layout.tsx`에서 HTML 주석으로 심어
빌드 산출물의 `<body>` 첫 자식으로 남겼는데, 공개된 포트폴리오라 페이지 소스를
연 사람에게 내부 작업 지침이 그대로 보였다. 감사 기록은 버전 관리되는 이 문서에
두는 편이 목적에 맞고, 방문자가 매 페이지마다 내려받을 이유도 없다.

```
IMPECCABLE DIRECTION CONTRACT · seed 77233364
THESIS: 이 포트폴리오는 읽는 문서가 아니라 제품 릴리스 피드다. 프로젝트마다 실제 화면이 먼저 오고 설명이 뒤따른다. 가운데 정렬 히어로와 3열 카드 그리드라는 카테고리 기본형을 거부한다.
OWN-WORLD: 블루블랙이 아닌 웜 그래파이트 다크(#131210)와 단일 앰버(#FFB224). 글로우와 유리 효과는 전면 금지. 채도는 오직 프로젝트 스크린샷이 낸다. Wanted Sans Variable 단일 패밀리로 무게(450~860) 대비만으로 위계를 만든다.
STORY: 방문자는 첫 화면에서 실제 만든 화면을 보고, 측정된 개선 세 건으로 신뢰를 얻고, 관심 프로젝트를 눌러 깊이 읽고, 이력서를 받거나 연락한다.
FIRST VIEWPORT: 좌측에 이름과 측정 지표, 우측에 실제 프로젝트 화면이 큰 스케일로. 기본 행동(프로젝트 보기)은 접힘 위에 둔다.
[미이행] "측정 지표"는 아직 첫 화면에 없다. 아래 '남은 것' 참고.
FORM: 사용자가 핀한 "모던 프로덕트" 세계. 롤은 grounded index 7을 배정했으나 핀이 롤을 이긴다.
FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, and DESIGN.md
```

## 색

CSS 변수는 알파를 붙일 수 있도록 공백 구분 RGB 채널로 둔다.

| 토큰 | 다크(기본) | 라이트 | 쓰임 |
|---|---|---|---|
| `--bg` | `19 18 16` | `250 248 244` | 페이지 바닥 |
| `--surface` | `34 31 28` | `255 255 255` | 카드 면 |
| `--surface-2` | `47 43 39` | `244 241 235` | 미디어 자리, 영상 플레이스홀더 |
| `--text` | `247 244 239` | `26 24 21` | 본문 |
| `--muted` | `170 164 156` | `107 101 92` | 보조 설명, 라벨 |
| `--border` | `62 57 51` | `220 214 204` | 1px 경계 |
| `--accent` | `255 178 36` | `146 77 4` | 행동, 포커스, 현재 상태 |
| `--accent-ink` | `23 21 15` | `255 252 246` | 앰버 위에 얹는 글자 |
| `--accent-ink-body` | `58 45 18` | `244 235 222` | 앰버 위 본문 |
| `--accent-ink-soft` | `94 68 21` | `235 221 202` | 앰버 위 라벨 |

`--surface`의 라이트 값은 `255 255 255`였다. 나머지가 전부 웜 틴트인 팔레트에서
순백만 계열 밖으로 튀어 `255 253 250`으로 잡았다. 배경과의 대비는 1.06 → 1.05로
거의 그대로고, 층위는 어차피 경계선이 만든다.

`--accent-ink-body`·`--accent-ink-soft`는 연락 섹션에서 `opacity-80`/`opacity-70`이
하던 일을 대신한다. 불투명도로 흐리게 만들면 최종 색이 대비 계산에 잡히지 않는데,
라이트 모드 라벨이 실제로는 3.95까지 떨어져 있었다 — 이 문서가 스스로 정한 바닥선
아래다. 섞인 결과를 토큰으로 못박아 두 모드 모두 실측값을 갖게 했다.

**핵심 규칙: 채도는 프로젝트 스크린샷이 낸다.** UI 크롬은 웜 그래파이트
무채색으로 물러난다. 블루블랙(`#0A0A0F`류)은 의도적으로 피했다 — 스크린샷의
따뜻한 색이 차가운 바닥 위에서 뜨기 때문이다.

**앰버는 흩뿌리지 않는다.** 행동(버튼·포커스링·현재 직장 배지·활성 탭)에만 쓰고,
화면 전체를 덮는 색면은 연락 섹션 한 곳뿐이다 (`Contact.tsx`). 색을 페이지
규모로 확약하는 지점이 거기 하나라는 것이 이 시스템의 규율이다.

**금지:** 글로우, 유리(glassmorphism), 보라·인디고, 네온, 그라디언트 배경.

### 대비 (실측)

| 조합 | 다크 | 라이트 |
|---|---|---|
| 본문 / 배경 | 17.06 | 16.70 |
| 보조 / 배경 | 7.40 | 5.44 |
| 앰버 / 배경 | 10.38 | 6.03 |
| 앰버잉크 / 앰버 | 10.12 | 6.24 |
| 앰버 본문 / 앰버 | 7.45 | 5.41 |
| 앰버 라벨 / 앰버 | 5.03 | 4.79 |

라이트의 앰버는 다크와 같은 값을 쓰면 4.38로 AA에 못 미쳐 `146 77 4`로 따로 잡았다.

아래 두 줄은 브라우저에서 실제 계산된 색으로 잰 값이다. **앰버 위에 글자를
얹을 때 불투명도를 쓰지 않는다** — 계산할 수 없는 값이 되고, 그러면 이 표를
유지할 수 없다.

## 타이포

**Wanted Sans Variable** 단일 패밀리 (SIL OFL, `wanted-sans` npm).
`variable/split` 서브셋을 `globals.css`에서 `@import`해 로컬 서빙한다 —
유니코드 레인지로 쪼개져 있어 필요한 조각만 내려간다.

위계는 색이 아니라 **무게 대비**로 만든다. 가변축 400–1000 중 실제로 쓰는 단계:

| 이름 | 값 | 쓰임 |
|---|---|---|
| `font-book` | 450 | 히어로 리드 문장 |
| `font-medium` | 550 | 버튼, 라벨, 태그 |
| `font-bold` | 700 | 카드·경력 제목 |
| `font-black` | 860 | h1, 섹션 제목 |

크기는 `clamp()`로 뷰포트에 연동한다. h1은 `clamp(2.75rem, 7vw, 4.5rem)`,
섹션 제목은 `clamp(1.6rem, 3.5vw, 2.25rem)`. 제목은 `tracking-[-0.03em]`으로
조인다. 숫자는 `time`과 `[data-numeric]`에서 `tabular-nums`.

### 줄바꿈

`body`에 `word-break: keep-all`과 `overflow-wrap: anywhere`를 짝으로 건다.

기본값(`word-break: normal`)에서 한글은 음절 사이 어디서나 끊긴다. "어떻게
만들었는 / 지"처럼 어절 중간이 잘리는데, 카드 본문이나 상세 페이지의 `20rem`
사이드바처럼 좁은 폭에서 계속 일어났다. `keep-all`이 어절을 묶는다.

`anywhere`가 없으면 이번엔 긴 영문 식별자나 URL이 컨테이너를 밀어낸다. 둘은
반드시 같이 간다. 이미 `break-all`을 명시한 곳(상세 페이지의 GitHub 주소)은
그 의도가 맞으므로 그대로 둔다.

## 레이아웃

### 세로 리듬

섹션마다 세로 여백이 다르다. 이건 취향이 아니라 규칙이다.

| 단계 | 값 | 쓰는 곳 |
|---|---|---|
| `wide` | `py-24 md:py-32` | 프로젝트 |
| `base` | `py-20 md:py-28` | 경력 |
| `close` | `py-16 md:py-24` | 기술 스택 |
| `quiet` | `py-12 md:py-16` | 학력 · 자격증 |

한때 다섯 구역이 전부 `py-16 md:py-24`였다. 여백이 같으면 스크롤 내내 같은
박자가 반복되고, 어느 구역이 중요한지 페이지가 말해 주지 못한다. 위에서
가장 넓게 열고 아래로 갈수록 조인 뒤 마지막 앰버 색면에서 다시 연다 —
프로젝트가 주인공이고 자격증은 확인용이라는 사실이 여백에 그대로 있다.

`Section` 컴포넌트의 `space` prop이 이 표를 갖는다. 새 섹션은 네 값 중
하나를 고른다. 새 값을 만들지 않는다.

### 폭

- `max-w-shell` = 78rem. 모든 섹션의 바깥 폭.
- `max-w-prose` = 42rem. 읽는 텍스트의 최대 폭.
- 상세 페이지 미디어는 `max-w-4xl`로 한 번 더 조인다 — shell 폭에 `aspect-video`를
  주면 700px가 넘어가 화면을 다 잡아먹는다.
- 히어로는 `lg:grid-cols-[minmax(0,1.08fr)_minmax(0,1fr)]` 비대칭. 좌측 텍스트가
  약간 넓어야 "Angular · Vue · React · Next.js" 줄이 깨지지 않는다.
- 히어로는 `min-h-[calc(100svh-4rem)]`에 `justify-center`로 첫 화면을 채우고,
  하단의 `animate-scroll-cue` 화살표가 아래에 더 있다고 알린다. `pb-24`가
  그 화살표의 자리다 — 줄이면 본문과 겹친다.

  이 세로 리듬 표에서 히어로는 예외다. 위 네 단계는 `Section`을 쓰는 구역에만
  적용된다.
- 상세 페이지는 본문 + `20rem` 사이드바, 사이드바는 `lg:sticky lg:top-24`.
- 라운드는 카드 `rounded-xl`(12px), 색면 `rounded-2xl`(16px), 버튼·태그 `rounded-full`.

## 컴포넌트 규약

- **컴포넌트 CSS 클래스를 만들지 않는다.** 전부 Tailwind 유틸리티로 처리해
  선택자가 서로 상쇄되는 사고를 원천 차단한다. `globals.css`에는 토큰과
  접근성 바닥선만 둔다.
- **카드는 스크린샷이 있는 것만 쓴다.** 이게 이 문서에서 가장 자주 어겨진
  규칙이었다. 프로젝트·경력·기술 스택·학력이 전부 `rounded-xl border
  bg-surface p-6`을 쓰다 보니, 실제 화면이 있는 것과 글자만 있는 것이 같은
  무게로 보였다. THESIS가 거부한다고 적어 둔 "카드 그리드"를 열 수만 줄여
  그대로 하고 있었던 셈이다.

  지금 `bg-surface` 상자를 갖는 것은 히어로 figure와 프로젝트 카드 넷,
  그리고 맨 위로 가기 버튼뿐이다. 나머지 구역은 가로선으로 행을 나눈다 —
  경력은 hairline 목록, 기술 스택은 라벨 좌측 열 + 항목 우측의 정의 목록,
  학력은 `max-w-3xl` 인덱스. 형태가 구역마다 다르니 스크롤에 리듬이 생긴다.
- 카드 면은 `bg-surface` + `border-border`. 층위는 그림자가 아니라 명도로 만든다
  (다크에서 카드/배경 대비 1.14, 경계/배경 1.64).
- 호버는 `hover:border-accent/60`과 이미지 `scale-[1.03]`. 그림자를 쓰지 않는다.
- **앰버 좌측 선은 한 곳에만.** 카드 하이라이트·상세 하이라이트·본문 h3
  세 곳이 같은 장치를 쓰고 있었다. 강조가 셋이면 강조가 아니다. 상세 페이지
  머리글 하나만 남기고, 카드는 가로선으로, 본문 소제목은 무게(`font-black`)로
  물러난다.
- 선 아이콘의 `strokeWidth`는 전부 `1.5`다. 1.4 / 1.5 / 1.75가 섞여 있었다.
- 프로젝트 카드 썸네일은 첫 이미지 미디어. 영상만 있는 프로젝트는 앰버 재생
  버튼이 놓인 `surface-2` 패널로 대체한다 — 빈 상자로 보이지 않게.

## 모션

- `animate-rise`: 히어로 진입 시 12px 상승 + 페이드, `cubic-bezier(0.22,1,0.36,1)` 0.5s.
- `[data-reveal]`: 스크롤 진입 시 20px 상승 + 페이드, 같은 곡선 0.9s.
- **Reveal은 순서가 내용상 참인 곳에만 건다** — 프로젝트 카드와 경력 목록
  둘뿐이다. `Section`이 제목을, `Skill`·`Education`이 내용 전체를 감싸던
  때가 있었는데, 그러면 홈의 거의 모든 덩어리가 스크롤마다 올라와 페이지가
  끝내 안착하지 않는다. "차례로 도착한다"는 뜻도 같이 사라진다. 구조에
  해당하는 것(섹션 제목, 목록이 아닌 내용)은 그냥 거기 있는다.
- 나머지는 `transition-colors` / `transition-transform` 수준으로 억제한다.
- `prefers-reduced-motion: reduce`에서 모든 애니메이션·트랜지션을 0.01ms로 죽인다.

## 테마

다크가 기본이고 `.light` 클래스가 라이트를 켠다. `layout.tsx`의 인라인 스크립트가
첫 페인트 전에 `localStorage.theme`을 읽어 깜빡임을 막는다. `color-scheme`도 함께
바꿔 스크롤바와 폼 컨트롤이 따라오게 한다. 토글은 `ThemeToggle.tsx`.

## 접근성 바닥선

- `html`·`body`에 `overflow-x: clip`. `hidden`이 아니어야 한다 — `hidden`은
  스크롤 컨테이너를 만들어 헤더와 상세 사이드바의 `position: sticky`를 죽인다.
- **누를 수 있는 글자는 두 줄이 되지 않는다.** 320px에서 "이력서 PDF
  내려받기"가 접혔다. 라벨을 줄이거나(`이력서 받기`) `whitespace-nowrap`으로
  못박는다. 줄이 바뀌면 눌러도 되는 것인지부터 흐려진다.
- `:focus-visible`에만 앰버 2px 링 + 오프셋. 마우스 클릭에는 링이 뜨지 않는다.
- 본문 건너뛰기 링크가 `<body>` 최상단에 있고 포커스 시에만 드러난다.
- 위 대비표의 값이 최소 기준이다. 새 색 조합은 여기에 맞춰야 한다.
- 영상은 클릭 전까지 로드하지 않는다. `poster`가 없어 자동 로드 시 검은 사각형만
  보이고, 일부 파일은 75MB라 첫 프레임까지 전체를 받아야 한다.

## 남은 것

**히어로의 "측정 지표".** 위 계약의 FIRST VIEWPORT와 STORY는 첫 화면에서
"측정된 개선 세 건으로 신뢰를 얻는다"고 적어 뒀는데, 실제로 첫 화면에 숫자가
있었던 적이 없다. 계약이 페이지보다 앞서 있는 상태다.

지어내지 않고 쓸 수 있는 숫자는 지금 저장소 안에 둘뿐이다.

- 빌드 시간 `20초 → 5초` (Vue CLI → Vite, 아이티센엔텍)
- 프로젝트 `11개` (`projects.length`에서 계산되므로 손으로 적지 않는다)

세 번째 자리를 채우려면 없는 숫자를 만들어야 한다. **숫자 모양의 빈칸은
정직하지만 지어낸 숫자는 슬롭이다** — 방문자는 꾸며낸 지표를 꾸며낸 후기만큼
빨리 알아본다. 그래서 두 건으로 줄이든, 실제 측정치를 하나 더 확보하든,
숫자 없이 가든 셋 중 하나를 고르는 일이 남아 있다. 고르는 것은 본인 몫이라
비워 뒀다.

## 알려진 부채

- `public/`이 162MB. `shuttle.mp4`가 75MB이고 moov atom이 파일 끝에 있어
  브라우저가 메타데이터조차 즉시 읽지 못한다. 압축 + `poster` 이미지 필요.
- 셔틀 프로젝트에 정지 이미지가 없어 카드가 대체 패널로 나온다. 대표 프로젝트인
  만큼 스크린샷 한 장이라도 확보하는 편이 낫다.
- `public/resume.pdf`가 아직 없다. 경로는 `app/content/site.ts`의 `RESUME_URL`.
